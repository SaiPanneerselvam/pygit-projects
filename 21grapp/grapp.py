# THIS IS A LIBRARY, NOT A FILE
# 2004 GRIFFAPPS MIT LICENCED \
def compile(var):
  if isinstance(var, str) = False:
    print("computer error. Value was not a string to compress. Value must be a string.")
def save(var1):
    var1 = saveslot
    print("saved")
def load(var2)
    saveslot = var2
    print("loaded into var2")
